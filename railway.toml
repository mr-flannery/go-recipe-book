[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
buildArgs = { COMMIT_HASH = "${{RAILWAY_GIT_COMMIT_SHA}}" }

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
